services:
  - type: web
    name: accessdrift
    runtime: docker
    plan: free
    dockerfilePath: ./Dockerfile
    healthCheckPath: /
    envVars:
      - key: PORT
        value: 8080
      - key: CHROME_PATH
        value: /usr/bin/chromium
